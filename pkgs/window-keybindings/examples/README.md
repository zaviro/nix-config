# Examples

本目录只展示 `window-keybindings` reference profile 与 helper 的调用方式，不参与任何
主机部署。

- `niri.kdl`：最小 Niri 调用示例，假设 helper 已在 `PATH`；实际 Nix 部署应使用绝对
  store path。

仓库根目录的 `index.html` 是上游保留的早期交互草案，不代表 v0.7；当前语义以
`SPEC.md` 与 `profiles/v0_7.nix` 为准。
